<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:orientation="vertical">

<ImageView
    android:id="@+id/location_image"
    android:layout_width="200dp"
    android:layout_height="100dp"
    android:contentDescription="@string/location_image_description"
    android:scaleType="centerCrop"
    android:src="@drawable/parking" /> <!-- Placeholder image -->


    <TextView
    android:id="@+id/location_title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/beach_name"
    android:textStyle="bold"
    android:textSize="16sp"
    android:padding="4dp" />

<TextView
    android:id="@+id/facilities"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/facilities_info"
    android:textSize="14sp"
    android:padding="2dp" />

<TextView
    android:id="@+id/reviews"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/reviews_info"
    android:textSize="14sp"
    android:padding="2dp" />

</LinearLayout>
